{
  "version": 2,
  "buildCommand": "npm run build:frontend && npx esbuild api/_server.ts --bundle --platform=node --format=cjs --outfile=api/index.js --packages=external",
  "outputDirectory": "dist/public",
  "functions": {
    "api/index.js": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/index.js"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}